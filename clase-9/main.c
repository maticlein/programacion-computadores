#include <stdio.h>

int main(){
    
    /*
    Inicio del código
    que va a permitir realizar cálculos 
    con números ingresados por el usuario
    a través de la línea de comandos
    */
    
    // Declaración de variables
    int a;
    int b; 
    int c; 
    int temperaturaFinal; // Variable para almacenar el resultado final

    // Mensaje de saludo
    printf("Hola!\n");
    printf("Ingrese un número: ");
    scanf("%i", &a);
    //b = a + temperaturaFinal;
    b = a + b * c;
    
    //printf("resultado");
    printf("resultado");

    return 0;
}