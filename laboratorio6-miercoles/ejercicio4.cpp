#include <stdio.h>

int edad;

int main(){
    // PEDIR EDAD (número entero)
    printf("ingrese ----");
    scanf("%d", &edad);
    // PEDIR DESTINO (número entero: 1 - 2 - 3)
    // REVISAR TABLA
    if(EDAD < 15){
        DESTINO == 1?
            printf("2500")
        DESTINO == 2?
            printf("1200")
        DESTINO == 3?
            printf("2000");
    } else if (edad < 65){
        DESTINO == 1?
        DESTINO == 2?
        DESTINO == 3?
    } else {
        DESTINO == 1?
        DESTINO == 2?
        DESTINO == 3?
    }
    return 0;
}